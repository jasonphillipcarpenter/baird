class Cli:
    def __init__(self):
        self.title = "mock_title"
        self.login = "mock_login"
        self.identityfile = "mock_identityfile"
        self.bastion = "mock_bastion"
        self.bastion_login = "mock_bastion_login"
        self.bastion_id = "mock_bastion_id"
        self.servers = ["mock_server_1", "mock_server_2"]
